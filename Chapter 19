def readposint():
    try:
        x = int(input("Enter a positive integer: "))
        if x <= 0:
            raise ValueError('%d is not a positive integer' % (x))
    except:
        print(x, "is not a valid entry")

readposint()
